<Window x:Class="Idler.AddTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Idler.ViewModel" 
        xmlns:ViewModels="clr-namespace:Idler.ViewModel;assembly=Idler.ViewModel"
        mc:Ignorable="d"
        Title="New Task" Height="150" Width="300">
    <Window.DataContext>
        <ViewModels:AddTaskVM/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10">
            <Label>Task Name</Label>
            <TextBox Text="{Binding TaskVM.Name}"></TextBox>
        </StackPanel>
        <StackPanel Grid.Column="2" Margin="10">
            <Label>Fixed time [h]</Label>
            <TextBox></TextBox>
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding OKCommand}" Width="50" Margin="10,5" HorizontalAlignment="Right">OK</Button>
                <Button Command="{Binding CancelCommand}" Width="50" Margin="1,5" HorizontalAlignment="Right">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
